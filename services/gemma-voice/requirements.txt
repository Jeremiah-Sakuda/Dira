fastapi==0.115.6
uvicorn[standard]==0.34.0
transformers>=5.10.1
torch>=2.6.0
accelerate>=1.3.0
soundfile>=0.13.0
numpy>=2.0.0
# Gemma 3n's vision tower is a TimmWrapperModel; transformers imports these
# even for audio-only use, so pipeline construction fails without them.
timm>=1.0.15
pillow>=11.0.0
# transformers' audio chat-template loading requires librosa at runtime.
librosa>=0.10.2
